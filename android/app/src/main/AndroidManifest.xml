<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- BLE mesh: Android 12+ (API 31+) runtime Bluetooth permissions.
         neverForLocation declares this app never derives physical location
         from scan results, so the system doesn't also require a location
         permission grant before BLUETOOTH_SCAN works. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"/>
    <!-- Legacy Bluetooth permissions for pre-12 (API <= 30) devices, where
         BLE scanning also required location — covered by the unbounded
         ACCESS_FINE_LOCATION below (flutter_compass requires it anyway). -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30"/>
    <!-- Required by flutter_compass on Android (magnetometer + location
         fusion; see its README's Android setup section) and doubles as the
         pre-12 BLE scan location requirement above. Runtime permission
         request flow (BLUETOOTH_SCAN/CONNECT + this) is still a
         device-pass TODO — no emulator/device available in this sandbox to
         verify the request UX. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- NFC (spec §4): reader/writer only. required="false" because HCE
         tap-mode is device-pass-blocked (no maintained Flutter HCE plugin
         as of 2026-08, see lib/adapters/nfc_adapter.dart) and NTAG sticker
         features degrade gracefully on hardware without NFC. No
         <apduservice> registration — add one alongside a real HCE
         implementation, not before. -->
    <uses-permission android:name="android.permission.NFC"/>
    <application
        android:label="cash_me_outside"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- HCE apduservice registration belongs here once a maintained
             plugin lands; see the NFC <uses-permission> comment above. -->
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
    <uses-feature android:name="android.hardware.nfc" android:required="false"/>
</manifest>
